<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".WelcomeActivity">

    <TextView
        android:id="@+id/tv_header"
        android:layout_width="match_parent"
        android:layout_height="230dp"
        android:text="The Weather"
        android:textSize="55sp"
        android:textColor="@color/white"
        android:background="@drawable/weatehr_img"
        android:gravity="center"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_description"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:layout_margin="8dp"
        android:textStyle="bold"
        android:gravity="center"
        android:text="Select your favorite countries"
        android:textSize="26sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tv_header"/>

    <TextView
        android:id="@+id/tv_region_options"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="#414E91"
        android:gravity="center"
        android:text="Start by choosing a region"
        android:textColor="@color/white"
        android:textSize="20sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_description" />

    <!--    spinner-->
    <Spinner
        android:id="@+id/spn_regions"
        android:layout_width="250dp"
        android:layout_height="40dp"
        android:layout_margin="10dp"
        android:background="#414E91"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_region_options" />

</androidx.constraintlayout.widget.ConstraintLayout>